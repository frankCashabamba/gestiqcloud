"""Module registry: discovery of module manifests and simple HTTP interfaces.

Each feature module may expose a `manifest.py` with a top-level `MANIFEST` that
conforms to `ModuleManifest` defined in `.models`.
"""

