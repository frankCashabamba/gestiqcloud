Catálogos de Timezones y Locales

- Crea tablas `ref_timezone(name TEXT PK, display_name TEXT, offset_minutes INT, active BOOLEAN)` y `ref_locale(code TEXT PK, name TEXT, active BOOLEAN)`.
- Semillas mínimas para uso inmediato en Admin.
