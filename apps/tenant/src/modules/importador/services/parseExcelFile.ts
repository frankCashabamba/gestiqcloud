export async function parseExcelFile(_file: File): Promise<{ headers: string[]; rows: Record<string,string>[] }> {
  // Stub: integrar xlsx (SheetJS) para leer Excel
  return { headers: [], rows: [] }
}

